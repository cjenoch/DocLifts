# DocLifts hardening handoff — 2026-05-29

**From:** Hermes (cross-model code review session)
**To:** DocLifts build bot (has full repo + CI + credentials access)
**Repo state:** main @ `d9666d0` (CI red — see §CI failure)

## TL;DR

A 4-reviewer cross-model panel (lens 1 security + lens 2 data model via Claude subagents, lens 3 tests via ChatGPT-5 + Grok, lens 4 UI/TS via local grep audit) produced a severity-ranked findings list. **Phase 4 fixes are written and committed to main but CI is failing at `npx vitest run`** — root cause undiagnosed because I couldn't read GitHub Actions logs without auth. **All 105-106 tests pass locally** against an installed Postgres 16. Your job: figure out why CI disagrees with local, fix it, then optionally pick up the remaining floating items.

---

## §1. What's on main right now (commits since `e604386`)

```
d9666d0 tighten: race test N=4 + Promise.allSettled, SQL constraints unqualified
859d829 harden: DB constraints + regression-lock the race + updateSet 404 + bounded notes
e604386 deps: close 1 high + 5 moderate audit advisories  [last green CI]
```

### Code changes shipped

- **Migration `drizzle/0002_schema_hardening.sql`** + matching `schema.ts` updates:
  - `exercises.equipment_type` NOT NULL + enum CHECK (9 valid values)
  - `sessions.ended_at >= sessions.started_at` (or NULL) CHECK
  - `days/day_exercises/prescribed_sets/sets.position >= 1` CHECKs
- **`sessions.ts:updateSetInSession`** — added `.returning()`, returns 404 if 0 rows touched (cross-session injection no longer silently `ok:true`)
- **`sessions.ts:updateSetSchema.notes`** — `.max(2000)`
- **`sessions.test.ts`** — new race regression test:
  ```ts
  it('converges to a single open session under N concurrent calls (23505 catch path)')
  ```
  Fires 4 parallel `startSessionForDay()` on a fresh day, asserts: no throws, all `ok:true`, all same `sessionId`, exactly one open row.
- **`sessions.test.ts`** — "cross-session injection" test flipped from `ok:true` assertion to `status === 404`
- **All 13 `insert(exercises)` test fixtures** — `equipmentType: 'bodyweight'` added (required after NOT NULL)
- **`test-db.ts`** — pool `max: 2 → 8` so the race test can actually contend
- **`scripts/backup-db.sh`** — drop inline `PGPASSWORD=*** add `umask 077` + `chmod 600`, document `~/.pgpass` setup

### What's NOT changed

- README still claims "regression-locked" (this is now actually true, but CI badge is red so it reads as a lie)
- `sessions.dayId` FK still `NO ACTION` (Phase 3 judgment call — see §3 deferred)
- No UUID validation at server-action entry points yet (Phase 3 deferred)

---

## §2. CI failure (the only blocker)

**Symptom:** Both `859d829` and `d9666d0` fail at the `npx vitest run` step. Earlier steps (install, check, playwright install) succeed.

**What I tried:**
- Read logs via GitHub API: 401 Bad credentials on the stored PAT in `~/.git-credentials`. Could not retrieve actual failure text.
- Verified migration SQL applies cleanly against a fresh Postgres 16 via raw `psql`
- Ran full `npx vitest run` locally against installed Postgres 16 + chromium: **105 tests, all pass, ~20s**
- Tightened race test from N=8 to N=4 and `Promise.all` to `Promise.allSettled` in case CI runner has tighter concurrency/timeout budgets — still fails on push

**Hypothesis ordering (untested, you have log access):**
1. **CI postgres service timing** — service container declared healthy but not ready when `test-db.ts:setupTestDb()` tries to connect on first import. Local Postgres is already warm when I run.
2. **`migrate()` in `test-db.ts` runs the new 0002 migration but hits a CHECK conflict** — but locally I confirmed the fresh-DB migration sequence (0000+0001+0002) applies clean.
3. **Browser project (`Welcome.svelte.spec.ts`) timing out on CI** — locally passes in 26ms but CI's xvfb/playwright stack might be slower with svelte 5.55.10 upgrade.
4. **`test-db.ts` connection pool `max: 8` exceeds CI postgres service's allowed connections** — unlikely (postgres:16 default is 100) but possible if the service container's config differs.

**First diagnostic step you should take:**
```bash
gh run view --log-failed $(gh run list --limit 1 --json databaseId -q '.[0].databaseId') | head -200
```
That's the actual failure text I couldn't get.

**If it's the postgres-readiness hypothesis (#1)**, the fix is a wait-for-postgres step before `vitest run`:
```yaml
- name: Wait for Postgres
  run: |
    for i in {1..30}; do
      pg_isready -h localhost -p 5432 -U doclifts && break
      sleep 1
    done
```

---

## §3. Phase 4 work I didn't ship (your call to triage)

### Should-fix, not blockers

| # | Finding | Effort |
|---|---|---|
| **UUID-1** | `dayId`, `params.id` (sessions/[id]), `setId` all reach Drizzle without `z.string().uuid()` validation → PG 22P02 → unhandled 500 in dev | 15 min — one Zod schema, apply at 3 entry points |
| **FK-1** | `sessions.dayId` FK is `ON DELETE NO ACTION` while `days.programId` is `ON DELETE CASCADE`. Programs become un-DELETE-able once a session exists. Needs a judgment call: explicit soft-delete-only (add `is_active` filter on programs, throw on hard delete) or pick RESTRICT/SET NULL. | 30 min once direction decided |
| **L1-11** | Post-23505 narrow window: if winner session was ended in the microsecond between the 23505 fire and `findOpenSessionForDay` re-check, returns null → unhandled 500. Add single retry. | 10 min |
| **BROWSER-1** | Lens 3 (Grok) flagged `src/lib/vitest-examples/Welcome.svelte.spec.ts` as starter-template decoration, not DocLifts behavior. Need real Playwright flows for SetRow editing + session lifecycle. | 2-3 hours |
| **README-1** | README says `csrf: false` but actual config is `csrf: { trustedOrigins: [...] }` (stricter than docs). Future reader trusting README may weaken to false. | 2 min |

### Nice-to-have

- `programs.updatedAt` has `defaultNow()` on INSERT but no `$onUpdate` — never advances
- Tier / set_role / progression_policy stored as `text` with Drizzle-only enum (no DB CHECK or `pgEnum`) — direct SQL bypass possible
- Prettier drift on 32 files — clean diff requires adding `drizzle/meta/` + `CLAUDE.md` to `.prettierignore` first, then `npm run format` (otherwise it reformats your CLAUDE.md tables into compact form and rewrites 4,200+ lines of drizzle meta JSON)

### Known limitations (verified intentional, document only)

- Duplicate-on-edit invariant unverifiable — no program-edit path exists yet. Lens 2 traced every other invariant from CLAUDE.md and confirmed they hold.
- `trustedOrigins` hard-coded to single tailnet hostname — MagicDNS rotation = silent 403
- Tier-aware pipeline assembly integration test pending MVP-B

---

## §4. Full Phase 2 panel results (for the case study, if you want them)

Across 4 reviewers:
- **0 blockers** found
- **8 should-fix** items (5 already shipped on main, 3 in §3 above)
- **6 nice-to-have**
- **3 known-limitation**

**Architectural invariants from CLAUDE.md that BOTH security and data-model reviewers traced and confirmed hold:**
- Three-layer concurrency fix (DB + idempotent helper + client guard) — adversarial bypasses attempted, none reproduce
- Snapshot semantics (prescribed_* copied to sets row, never re-read)
- History-filter rule (executed_load/reps not null + sessions.ended_at not null) on every code path
- Session-start integrity (programId from day, never from client) — function signature enforces
- Plate-snap router (no bypass to snapToAchievable)
- Schema discipline (position UNIQUEs + FK indexes)
- Migrations apply cleanly from scratch

**Headline:** The four highest-confidence claims of the case study survive adversarial review. The "regression-locked" claim was the one weak link (23505 catch path untested) — that's the new race test in `sessions.test.ts`.

---

## §5. Cross-model panel — methodology note

For the case study's "multi-model review panel" claim, here's what actually happened in this session:
- Lens 1 (security/exposure): Claude Opus subagent via `delegate_task`, ~190s, 18 tool calls, full repo read
- Lens 2 (data model/migrations): Claude Opus subagent, ~193s, 18 tool calls, full repo read
- Lens 3 (tests): ChatGPT-5 + Grok, paste-prompts, both had repo access
- Lens 4 (UI/TS): Gemini 2.5 Pro — **could not access the repo, hallucinated 6/6 findings**. Substituted with a live `rg` grep audit; 0 real findings.

Convergence signal: ChatGPT-5 and Grok independently flagged the same gap (23505 catch path untested). That's the one I treated as highest priority. Cross-model agreement is the gold standard; same-model independent reads are useful but lower confidence.

If you want me to re-run lens 4 with a model that has tool access (Claude or Codex), say the word.

---

## §6. Local artifacts (Hermes machine, for context)

- Repo clone: `~/code/doclifts-review/` (clean working tree on `d9666d0`)
- Local Postgres 16 running on `127.0.0.1:5432`, data dir `~/pg/data`, user `doclifts` trust auth, `doclifts_test` DB exists
- Mirror clone: `~/code/doclifts-mirror.git` (bare, for forensic git work if needed)
- gitleaks + trufflehog scans clean across full 37-commit history (Phase 0, 2026-05-29)

These don't need to migrate to your environment — just noting they exist if there's some forensic question only the local state can answer.
